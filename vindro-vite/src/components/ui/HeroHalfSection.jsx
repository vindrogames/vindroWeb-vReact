function HeroHalfSection({ title, subtitle }) {

    return (
        <section className="hero-half">
            <h1>{title}</h1>
            <h2>{subtitle}</h2>
        </section>
    )

}

export default HeroHalfSection;